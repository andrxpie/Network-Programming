<Window x:Class="SMTP_test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SMTP_test"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel Margin="10">
        <TabControl x:Name="tabs" Margin="0 0 0 10" SelectionChanged="Select_Tab">
            <TabItem Header="Authorize"/>
            <TabItem Header="Send" IsEnabled="{Binding authorize.IsAuthenticated}"/>
        </TabControl>

        <Frame x:Name="frame" NavigationUIVisibility="Hidden"/>
    </StackPanel>
</Window>